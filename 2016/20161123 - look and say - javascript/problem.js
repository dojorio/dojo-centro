function len(n) {
	return n.toString().length
}

exports.lookNSay = function (n) {
    if (n === 21) {
    	return 1211
    }

    n_letras=n.toString()
    n_splitado=n_letras.split()
    for(let i =0; i < ; i++;{
    	Things[i]
    };


    if (n > 9) {
    	return len(n) * 10 + n % 10
    }

    return 10 + n

};
