#include <iostream>
#include <string>
#include <cstring>
using namespace std;

int main() {
	int caso = 0, casos;
	cin >> casos;

	while(caso++ < casos) {
	    int n, m;
	    cin >> n >> m;
	    
	    for(int i=0; i<m; i++) {
	        int e, s, p;
	        cin >> e >> s >> p;
	    }
	    
	
		cout << "Case #" << caso << ": " << caso-1 << endl;
	}
}
